{
  "name": "redux-epic-utils",
  "version": "0.0.0",
  "main": "src/lib.js",
  "dependencies": {
    "redux-observable": "^1.0.0",
    "rxjs": "^6.0.0"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-google": "^0.12.0",
    "jest": "^24.1.0"
  },
  "scripts": {
    "lint": "eslint -c .eslintrc src/",
    "test": "jest --config=jest.json"
  }
}
